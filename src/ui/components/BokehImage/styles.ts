/** @format */

import styled from "@emotion/native";

export const Image = styled.Image(() => ({
  flex: 1,
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
  width: "100%",
  height: "100%",
  position: "absolute",
}));
